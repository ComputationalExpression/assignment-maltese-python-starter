[project]
name = "maltese-python"
version = "1.0.0"
requires-python= ">=3.10"
dependencies = [
    "capturer",
    "gatorgrade",
]

[tool.uv.sources]
capturer = { git = "https://github.com/ComputationalExpression/Capturer" }
